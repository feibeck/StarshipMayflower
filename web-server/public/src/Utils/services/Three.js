define([
    '../module',
    'three',
    'orbit-controls'
], function (module, THREE) {
    'use strict';

    module.factory('THREE', [
        function()
        {
            return THREE;
        }
    ]);

});